package com.productos; // En el paquete 'calculadores'
 
import org.openxava.calculators.*; // Para usar 'ICalculator'
 
public class CalculadorServicio implements ICalculator {
 
    public Object calculate() throws Exception {
    	boolean numero;
            numero = false;
        return numero;
    }
}